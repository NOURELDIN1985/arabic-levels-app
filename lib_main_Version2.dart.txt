import 'package:flutter/material.dart';
import 'screens/levels_list.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Arabic Levels',
      theme: ThemeData(
        primarySwatch: Colors.blue,
        fontFamily: null, // ضع اسم الخط العربي هنا إن أردت
      ),
      home: const LevelsListScreen(),
    );
  }
}